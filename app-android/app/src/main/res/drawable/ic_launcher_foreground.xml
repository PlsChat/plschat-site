<?xml version="1.0" encoding="utf-8"?>
<!-- Launcher foreground: the real PLSChat logo, inset so the hexagon sits inside
     the adaptive-icon safe zone. The logo art already carries the navy backdrop
     that matches ic_launcher_background, so the inset border stays seamless. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_logo"
    android:inset="16%" />
