# Release builds set minifyEnabled false, so these rules are not applied today.
# They are kept as a hook in case shrinking is enabled later. The app is
# framework-only (no reflection-based libraries), so no keep rules are required.
